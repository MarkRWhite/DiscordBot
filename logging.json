{
    "version": 1,
    "disable_existing_loggers": false,
    "formatters": {
        "standard": {
            "format": "%(asctime)s - %(levelname)s - %(message)s",
            "datefmt": "%Y-%m-%d %H:%M:%S"
        }
    },
    "handlers": {
        "default": {
            "class": "logging.FileHandler",
            "formatter": "standard",
            "filename": "logging/{date}_manager.log",
            "mode": "a"
        }
    },
    "root": {
        "handlers": ["default"],
        "level": "INFO"
    }
}